{
    "name": "idct/buffered-tapefile-callback-reader",
    "description": "Buffered reader of string files which can be interpreted as tapes, which we always parse by moving forward. Based on specific cases a callback returning expected data can be fired.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^7.0"
    },
    "authors": [
        {
            "name": "IDCT Bartosz Pachołek",
            "email": "bartosz@idct.pl"
        }
    ],
    "autoload": {
        "psr-4": {
            "IDCT\\": "src/"
        }
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^2.16"
    },
    "scripts": {
        "fix": [
            "bash fix-code.sh"
        ]
    },
    "minimum-stability": "dev"
}
